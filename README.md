# POP_SPY570

## Streamlit (TrendScout)

```bash
streamlit run TrendScout.py
```

## Web app (Vite + API)

From the repository root:

```bash
npm install
npm run dev
```

Runs the dev server and Vite together (see `package.json`).
